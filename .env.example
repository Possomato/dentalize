# Database Connection
# Note: For Vercel deployment, you must use a cloud database (Postgres, MySQL, etc.) instead of SQLite.
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# NextAuth.js
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-at-least-32-chars"
# In Vercel, AUTH_URL is automatically detected, but you can set it if needed (e.g. for preview branches with specific auth needs)
# AUTH_URL="https://your-app.vercel.app"

# Third Party Integrations (Add these if you implement them)
# STRIPE_SECRET_KEY="sk_live_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."
# HUBSPOT_ACCESS_TOKEN="pat-na1-..."

# Client-side public variables (must start with NEXT_PUBLIC_)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
